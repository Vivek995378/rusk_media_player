abstract final class AppDurations {
  static const splash = Duration(milliseconds: 300);
  static const splashLogo = Duration(milliseconds: 800);
  static const splashText = Duration(milliseconds: 600);
  static const splashTagline = Duration(milliseconds: 500);
  static const splashExit = Duration(milliseconds: 400);
  static const splashBackground = Duration(milliseconds: 4000);
  static const splashParticle = Duration(seconds: 10);

  static const pageTransition = Duration(milliseconds: 250);
  static const pageTransitionReverse = Duration(milliseconds: 200);

  static const heartAnimation = Duration(milliseconds: 1200);
  static const playPauseIndicator = Duration(milliseconds: 700);
  static const overlayAnimation = Duration(milliseconds: 600);
  static const buttonScale = Duration(milliseconds: 250);
  static const followButtonScale = Duration(milliseconds: 300);
  static const followButtonContainer = Duration(milliseconds: 250);

  static const shimmer = Duration(milliseconds: 2000);
  static const shimmerPulse = Duration(milliseconds: 1500);
  static const shimmerParticle = Duration(seconds: 6);
  static const bufferingBar = Duration(milliseconds: 1200);

  static const progressScrubber = Duration(milliseconds: 200);

  static const paywallEntry = Duration(milliseconds: 700);
  static const paywallShimmer = Duration(milliseconds: 2500);
  static const paywallBorder = Duration(milliseconds: 3000);

  static const volumeFade = Duration(milliseconds: 250);

  static const snackbarSlide = Duration(milliseconds: 400);
  static const snackbarShake = Duration(milliseconds: 500);
  static const snackbarDisplay = Duration(seconds: 2);

  static const loaderMain = Duration(seconds: 4);
  static const loaderTypewriter = Duration(milliseconds: 60);
  static const loaderTypingDelay = Duration(milliseconds: 55);
  static const loaderMessagePause = Duration(milliseconds: 1800);
  static const loaderMessageFade = Duration(milliseconds: 400);

  static const robotBlink = Duration(milliseconds: 120);
  static const robotGlow = Duration(milliseconds: 1200);
  static const robotLook = Duration(milliseconds: 600);
  static const robotMouth = Duration(milliseconds: 300);
  static const robotMouthLoop = Duration(milliseconds: 1800);
  static const robotBlinkDelay = Duration(milliseconds: 2000);
  static const robotLookDelay = Duration(milliseconds: 400);
  static const robotLookCenter = Duration(milliseconds: 300);
  static const robotBlinkCenterDelay = Duration(milliseconds: 800);

  static const cursorBlink = Duration(milliseconds: 500);

  static const networkDelay = Duration(milliseconds: 300);
  static const controllerCleanupDelay = Duration(milliseconds: 50);
}
